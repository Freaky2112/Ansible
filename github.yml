---

- host: Test
  - name: Example clone of a single branch
    ansible.builtin.git:
      repo: https://github.com/Freaky2112/Aliases.git
      dest: /home/freaky/
      single_branch: yes
      version: master
